---
page_title: "ory_email_template Resource - ory"
subcategory: ""
description: |-
  Manages an Ory Network email template.
---

# ory_email_template (Resource)

Manages an Ory Network email template.

Email templates use [Go template syntax](https://pkg.go.dev/text/template) for variable substitution. HTML bodies use `html/template` (auto-escaping) and plaintext bodies use `text/template`. [Sprig template functions](http://masterminds.github.io/sprig/) are available, except date, random, OS, and network functions.

## Template Types

| Template Type | Flow | Description |
|---------------|------|-------------|
| `registration_code_valid` | Registration | Sent when user registers with a valid code |
| `registration_code_invalid` | Registration | Sent when registration code is invalid/expired |
| `login_code_valid` | Login | Sent when user logs in with a valid code |
| `login_code_invalid` | Login | Sent when login code is invalid/expired |
| `verification_code_valid` | Verification | Sent for email verification with valid code |
| `verification_code_invalid` | Verification | Sent when verification code is invalid/expired |
| `recovery_code_valid` | Recovery | Sent for account recovery with valid code |
| `recovery_code_invalid` | Recovery | Sent when recovery code is invalid/expired |
| `verification_valid` | Verification | Legacy verification email (link-based) |
| `verification_invalid` | Verification | Legacy verification invalid (link-based) |
| `recovery_valid` | Recovery | Legacy recovery email (link-based) |
| `recovery_invalid` | Recovery | Legacy recovery invalid (link-based) |

## Template Variables

Each template type has access to different variables:

| Template | Available Variables |
|----------|-------------------|
| `recovery_code_valid` | `{{ .To }}`, `{{ .RecoveryCode }}`, `{{ .Identity }}`, `{{ .ExpiresInMinutes }}` |
| `recovery_code_invalid` | `{{ .To }}` |
| `verification_code_valid` | `{{ .To }}`, `{{ .VerificationCode }}`, `{{ .VerificationURL }}`, `{{ .Identity }}`, `{{ .ExpiresInMinutes }}` |
| `verification_code_invalid` | `{{ .To }}` |
| `login_code_valid` | `{{ .To }}`, `{{ .LoginCode }}`, `{{ .Identity }}`, `{{ .ExpiresInMinutes }}` |
| `login_code_invalid` | `{{ .To }}` |
| `registration_code_valid` | `{{ .To }}`, `{{ .RegistrationCode }}`, `{{ .Traits }}`, `{{ .ExpiresInMinutes }}` |
| `registration_code_invalid` | `{{ .To }}` |
| `recovery_valid` | `{{ .To }}`, `{{ .RecoveryURL }}`, `{{ .Identity }}`, `{{ .ExpiresInMinutes }}` |
| `verification_valid` | `{{ .To }}`, `{{ .VerificationURL }}`, `{{ .Identity }}`, `{{ .ExpiresInMinutes }}` |

The `{{ .Identity }}` object provides access to `{{ .Identity.traits }}` and `{{ .Identity.metadata_public }}`.

## Important Behaviors

- **Destroying resets to defaults:** Deleting this resource resets the template to Ory's built-in default template. It does not leave a blank template.
- **Base64 encoding is automatic:** You provide raw template content; the provider handles base64 encoding internally.
- **Subject is optional:** If not specified, Ory uses a default subject line.

## Example Usage

```terraform
# Recovery code email
resource "ory_email_template" "recovery_code" {
  template_type = "recovery_code_valid"
  subject       = "Reset your password"

  body_html = <<-HTML
    <!DOCTYPE html>
    <html>
    <body>
      <h1>Password Reset</h1>
      <p>Hi {{ .Identity.traits.name.first }},</p>
      <p>Your recovery code is: <strong>{{ .RecoveryCode }}</strong></p>
      <p>This code expires in {{ .ExpiresInMinutes }} minutes.</p>
    </body>
    </html>
  HTML

  body_plaintext = <<-TEXT
    Password Reset

    Your recovery code is: {{ .RecoveryCode }}

    This code expires in {{ .ExpiresInMinutes }} minutes.
  TEXT
}

# Verification code email
resource "ory_email_template" "verification_code" {
  template_type = "verification_code_valid"
  subject       = "Verify your email address"

  body_html = <<-HTML
    <!DOCTYPE html>
    <html>
    <body>
      <h1>Email Verification</h1>
      <p>Your verification code is: <strong>{{ .VerificationCode }}</strong></p>
    </body>
    </html>
  HTML

  body_plaintext = <<-TEXT
    Email Verification
    Your verification code is: {{ .VerificationCode }}
  TEXT
}

# Login code email
resource "ory_email_template" "login_code" {
  template_type = "login_code_valid"
  subject       = "Your login code"

  body_html = <<-HTML
    <!DOCTYPE html>
    <html>
    <body>
      <h1>Login Code</h1>
      <p>Your login code is: <strong>{{ .LoginCode }}</strong></p>
      <p>This code expires in {{ .ExpiresInMinutes }} minutes.</p>
    </body>
    </html>
  HTML

  body_plaintext = <<-TEXT
    Your login code is: {{ .LoginCode }}
    This code expires in {{ .ExpiresInMinutes }} minutes.
  TEXT
}
```

## Import

```shell
terraform import ory_email_template.recovery recovery_code_valid
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `body_html` (String) HTML body template using Go `html/template` syntax.
- `body_plaintext` (String) Plaintext body template using Go `text/template` syntax.
- `template_type` (String) The email template type. Valid values: `registration_code_valid`, `registration_code_invalid`, `login_code_valid`, `login_code_invalid`, `verification_code_valid`, `verification_code_invalid`, `recovery_code_valid`, `recovery_code_invalid`, `verification_valid`, `verification_invalid`, `recovery_valid`, `recovery_invalid`.

### Optional

- `project_id` (String) Project ID. If not set, uses provider's project_id. **Cannot be changed after creation.**
- `subject` (String) Email subject template (Go template syntax). Optional; Ory uses a default if not specified.

### Read-Only

- `id` (String) Resource ID (same as template_type).
